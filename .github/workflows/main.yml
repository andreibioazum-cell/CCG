name: Pocket_Code_RU_Fixed
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Create Project
        run: |
          mkdir -p app/src/main/java/com/pocketru/fixed
          mkdir -p app/src/main/res/layout
          mkdir -p app/src/main/res/values
          mkdir -p app/src/main/res/drawable

          echo "android.useAndroidX=true" > gradle.properties
          echo "android.enableJetifier=true" >> gradle.properties

          cat <<EOF > app/src/main/res/values/themes.xml
          <resources>
            <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
              <item name="android:windowBackground">#F3F8FC</item>
            </style>
          </resources>
          EOF

          cat <<EOF > app/src/main/res/drawable/bg_panel.xml
          <shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
            <solid android:color="#FFFFFF"/>
            <corners android:radius="16dp"/>
            <stroke android:width="1dp" android:color="#D6E6F2"/>
          </shape>
          EOF

          cat <<EOF > app/src/main/res/drawable/bg_stage.xml
          <shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
            <solid android:color="#D8F0FF"/>
            <corners android:radius="18dp"/>
            <stroke android:width="2dp" android:color="#8AC4E8"/>
          </shape>
          EOF

          cat <<EOF > app/src/main/res/drawable/btn_blue.xml
          <shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
            <solid android:color="#4C97FF"/>
            <corners android:radius="14dp"/>
          </shape>
          EOF

          cat <<EOF > app/src/main/res/drawable/btn_green.xml
          <shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
            <solid android:color="#5FD36E"/>
            <corners android:radius="14dp"/>
          </shape>
          EOF

          cat <<EOF > app/src/main/res/drawable/btn_orange.xml
          <shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
            <solid android:color="#FFAB19"/>
            <corners android:radius="14dp"/>
          </shape>
          EOF

          cat <<EOF > app/src/main/res/drawable/btn_red.xml
          <shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
            <solid android:color="#FF6F6F"/>
            <corners android:radius="14dp"/>
          </shape>
          EOF

          cat <<EOF > app/src/main/res/drawable/btn_purple.xml
          <shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
            <solid android:color="#9966FF"/>
            <corners android:radius="14dp"/>
          </shape>
          EOF

          cat <<EOF > app/src/main/res/drawable/block_event.xml
          <shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
            <solid android:color="#FFBF00"/>
            <corners android:radius="16dp"/>
          </shape>
          EOF

          cat <<EOF > app/src/main/res/drawable/block_motion.xml
          <shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
            <solid android:color="#4C97FF"/>
            <corners android:radius="16dp"/>
          </shape>
          EOF

          cat <<EOF > app/src/main/res/drawable/block_looks.xml
          <shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
            <solid android:color="#9966FF"/>
            <corners android:radius="16dp"/>
          </shape>
          EOF

          cat <<EOF > app/src/main/res/drawable/block_control.xml
          <shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
            <solid android:color="#FFAB19"/>
            <corners android:radius="16dp"/>
          </shape>
          EOF

          cat <<'LAYOUT' > app/src/main/res/layout/activity_main.xml
          <?xml version="1.0" encoding="utf-8"?>
          <FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:background="#F3F8FC">

              <LinearLayout
                  android:id="@+id/screen_projects"
                  android:layout_width="match_parent"
                  android:layout_height="match_parent"
                  android:orientation="vertical"
                  android:padding="12dp">

                  <TextView
                      android:layout_width="match_parent"
                      android:layout_height="wrap_content"
                      android:text="Проекты"
                      android:textSize="26sp"
                      android:textStyle="bold"
                      android:textColor="#2D678F"/>

                  <Button
                      android:id="@+id/btn_new_project"
                      android:layout_width="match_parent"
                      android:layout_height="48dp"
                      android:text="+ Новый проект"
                      android:textColor="#FFFFFF"
                      android:background="@drawable/btn_blue"
                      android:layout_marginTop="12dp"/>

                  <ScrollView
                      android:layout_width="match_parent"
                      android:layout_height="0dp"
                      android:layout_weight="1"
                      android:layout_marginTop="10dp">

                      <LinearLayout
                          android:id="@+id/projects_list"
                          android:layout_width="match_parent"
                          android:layout_height="wrap_content"
                          android:orientation="vertical"/>
                  </ScrollView>
              </LinearLayout>

              <LinearLayout
                  android:id="@+id/screen_scenes"
                  android:layout_width="match_parent"
                  android:layout_height="match_parent"
                  android:orientation="vertical"
                  android:padding="12dp"
                  android:visibility="gone">

                  <LinearLayout
                      android:layout_width="match_parent"
                      android:layout_height="wrap_content"
                      android:orientation="horizontal">

                      <Button
                          android:id="@+id/back_to_projects"
                          android:layout_width="wrap_content"
                          android:layout_height="44dp"
                          android:text="←"
                          android:textColor="#FFFFFF"
                          android:background="@drawable/btn_red"/>

                      <TextView
                          android:id="@+id/txt_project_title"
                          android:layout_width="0dp"
                          android:layout_height="wrap_content"
                          android:layout_weight="1"
                          android:text="Сцены"
                          android:textSize="24sp"
                          android:textStyle="bold"
                          android:textColor="#2D678F"
                          android:gravity="center_vertical"
                          android:paddingLeft="12dp"/>
                  </LinearLayout>

                  <Button
                      android:id="@+id/btn_new_scene"
                      android:layout_width="match_parent"
                      android:layout_height="48dp"
                      android:text="+ Новая сцена"
                      android:textColor="#FFFFFF"
                      android:background="@drawable/btn_purple"
                      android:layout_marginTop="12dp"/>

                  <ScrollView
                      android:layout_width="match_parent"
                      android:layout_height="0dp"
                      android:layout_weight="1"
                      android:layout_marginTop="10dp">

                      <LinearLayout
                          android:id="@+id/scenes_list"
                          android:layout_width="match_parent"
                          android:layout_height="wrap_content"
                          android:orientation="vertical"/>
                  </ScrollView>
              </LinearLayout>

              <LinearLayout
                  android:id="@+id/screen_objects"
                  android:layout_width="match_parent"
                  android:layout_height="match_parent"
                  android:orientation="vertical"
                  android:padding="12dp"
                  android:visibility="gone">

                  <LinearLayout
                      android:layout_width="match_parent"
                      android:layout_height="wrap_content"
                      android:orientation="horizontal">

                      <Button
                          android:id="@+id/back_to_scenes"
                          android:layout_width="wrap_content"
                          android:layout_height="44dp"
                          android:text="←"
                          android:textColor="#FFFFFF"
                          android:background="@drawable/btn_red"/>

                      <TextView
                          android:id="@+id/txt_scene_title"
                          android:layout_width="0dp"
                          android:layout_height="wrap_content"
                          android:layout_weight="1"
                          android:text="Объекты"
                          android:textSize="24sp"
                          android:textStyle="bold"
                          android:textColor="#2D678F"
                          android:gravity="center_vertical"
                          android:paddingLeft="12dp"/>

                      <Button
                          android:id="@+id/btn_play_scene"
                          android:layout_width="wrap_content"
                          android:layout_height="44dp"
                          android:text="Запуск"
                          android:textColor="#FFFFFF"
                          android:background="@drawable/btn_green"/>
                  </LinearLayout>

                  <Button
                      android:id="@+id/btn_new_object"
                      android:layout_width="match_parent"
                      android:layout_height="48dp"
                      android:text="+ Новый объект"
                      android:textColor="#FFFFFF"
                      android:background="@drawable/btn_blue"
                      android:layout_marginTop="12dp"/>

                  <ScrollView
                      android:layout_width="match_parent"
                      android:layout_height="0dp"
                      android:layout_weight="1"
                      android:layout_marginTop="10dp">

                      <LinearLayout
                          android:id="@+id/objects_list"
                          android:layout_width="match_parent"
                          android:layout_height="wrap_content"
                          android:orientation="vertical"/>
                  </ScrollView>
              </LinearLayout>

              <LinearLayout
                  android:id="@+id/screen_scripts"
                  android:layout_width="match_parent"
                  android:layout_height="match_parent"
                  android:orientation="vertical"
                  android:padding="12dp"
                  android:visibility="gone">

                  <LinearLayout
                      android:layout_width="match_parent"
                      android:layout_height="wrap_content"
                      android:orientation="horizontal">

                      <Button
                          android:id="@+id/back_to_objects"
                          android:layout_width="wrap_content"
                          android:layout_height="44dp"
                          android:text="←"
                          android:textColor="#FFFFFF"
                          android:background="@drawable/btn_red"/>

                      <TextView
                          android:id="@+id/txt_object_title"
                          android:layout_width="0dp"
                          android:layout_height="wrap_content"
                          android:layout_weight="1"
                          android:text="Скрипты"
                          android:textSize="24sp"
                          android:textStyle="bold"
                          android:textColor="#2D678F"
                          android:gravity="center_vertical"
                          android:paddingLeft="12dp"/>
                  </LinearLayout>

                  <LinearLayout
                      android:layout_width="match_parent"
                      android:layout_height="0dp"
                      android:layout_weight="1"
                      android:orientation="vertical"
                      android:background="@drawable/bg_panel"
                      android:padding="10dp"
                      android:layout_marginTop="10dp">

                      <LinearLayout
                          android:layout_width="match_parent"
                          android:layout_height="wrap_content"
                          android:orientation="horizontal">

                          <TextView
                              android:layout_width="0dp"
                              android:layout_height="wrap_content"
                              android:layout_weight="1"
                              android:text="Блоки"
                              android:textStyle="bold"
                              android:textSize="18sp"
                              android:textColor="#356D99"/>

                          <Button
                              android:id="@+id/btn_add_block"
                              android:layout_width="44dp"
                              android:layout_height="44dp"
                              android:text="+"
                              android:textColor="#FFFFFF"
                              android:textSize="24sp"
                              android:background="@drawable/btn_blue"/>
                      </LinearLayout>

                      <ScrollView
                          android:layout_width="match_parent"
                          android:layout_height="0dp"
                          android:layout_weight="1"
                          android:layout_marginTop="8dp">

                          <LinearLayout
                              android:id="@+id/blocks_list"
                              android:layout_width="match_parent"
                              android:layout_height="wrap_content"
                              android:orientation="vertical"/>
                      </ScrollView>
                  </LinearLayout>
              </LinearLayout>

              <FrameLayout
                  android:id="@+id/screen_player"
                  android:layout_width="match_parent"
                  android:layout_height="match_parent"
                  android:visibility="gone"
                  android:background="#101820">

                  <FrameLayout
                      android:id="@+id/player_stage"
                      android:layout_width="match_parent"
                      android:layout_height="match_parent"
                      android:layout_margin="12dp"
                      android:background="@drawable/bg_stage">

                      <FrameLayout
                          android:id="@+id/player_stage_layer"
                          android:layout_width="match_parent"
                          android:layout_height="match_parent"/>
                  </FrameLayout>

                  <Button
                      android:id="@+id/btn_stop_player"
                      android:layout_width="80dp"
                      android:layout_height="48dp"
                      android:layout_margin="16dp"
                      android:text="Стоп"
                      android:textColor="#FFFFFF"
                      android:background="@drawable/btn_red"/>
              </FrameLayout>
          </FrameLayout>
          LAYOUT

          cat <<EOF > app/src/main/java/com/pocketru/fixed/BlockItem.java
          package com.pocketru.fixed;

          public class BlockItem {
              public String type;
              public String label;
              public String arg1;
              public String arg2;

              public BlockItem(String type, String label, String arg1, String arg2){
                  this.type = type;
                  this.label = label;
                  this.arg1 = arg1;
                  this.arg2 = arg2;
              }
          }
          EOF

          cat <<EOF > app/src/main/java/com/pocketru/fixed/ObjectItem.java
          package com.pocketru.fixed;

          import java.util.ArrayList;

          public class ObjectItem {
              public String name = "Объект";
              public String emoji = "🐱";
              public String imageUri = "";
              public float startX = 100f;
              public float startY = 80f;
              public float x = 100f;
              public float y = 80f;
              public boolean visible = true;
              public String say = "";
              public ArrayList<BlockItem> blocks = new ArrayList<>();
          }
          EOF

          cat <<EOF > app/src/main/java/com/pocketru/fixed/SceneItem.java
          package com.pocketru.fixed;

          import java.util.ArrayList;

          public class SceneItem {
              public String name = "Сцена";
              public String bgColor = "#D8F0FF";
              public ArrayList<ObjectItem> objects = new ArrayList<>();
          }
          EOF

          cat <<EOF > app/src/main/java/com/pocketru/fixed/ProjectItem.java
          package com.pocketru.fixed;

          import java.util.ArrayList;

          public class ProjectItem {
              public String name = "Проект";
              public ArrayList<SceneItem> scenes = new ArrayList<>();
          }
          EOF

          cat <<'STORE' > app/src/main/java/com/pocketru/fixed/ProjectStore.java
          package com.pocketru.fixed;

          import android.content.Context;
          import org.json.JSONArray;
          import org.json.JSONObject;
          import java.io.*;
          import java.util.ArrayList;

          public class ProjectStore {
              private static File file(Context c){
                  return new File(c.getFilesDir(), "projects_ru_fixed.json");
              }

              public static ArrayList<ProjectItem> load(Context c){
                  ArrayList<ProjectItem> out = new ArrayList<>();
                  try{
                      File f = file(c);
                      if(!f.exists()) return out;
                      BufferedReader rd = new BufferedReader(new FileReader(f));
                      StringBuilder sb = new StringBuilder();
                      String line;
                      while((line = rd.readLine()) != null) sb.append(line);
                      rd.close();

                      JSONArray arr = new JSONArray(sb.toString());
                      for(int i=0;i<arr.length();i++){
                          JSONObject p = arr.getJSONObject(i);
                          ProjectItem pr = new ProjectItem();
                          pr.name = p.optString("name", "Проект");

                          JSONArray scenes = p.optJSONArray("scenes");
                          if(scenes != null){
                              for(int j=0;j<scenes.length();j++){
                                  JSONObject s = scenes.getJSONObject(j);
                                  SceneItem sc = new SceneItem();
                                  sc.name = s.optString("name", "Сцена");
                                  sc.bgColor = s.optString("bgColor", "#D8F0FF");

                                  JSONArray objs = s.optJSONArray("objects");
                                  if(objs != null){
                                      for(int k=0;k<objs.length();k++){
                                          JSONObject o = objs.getJSONObject(k);
                                          ObjectItem ob = new ObjectItem();
                                          ob.name = o.optString("name", "Объект");
                                          ob.emoji = o.optString("emoji", "🐱");
                                          ob.imageUri = o.optString("imageUri", "");
                                          ob.startX = (float)o.optDouble("startX", 100);
                                          ob.startY = (float)o.optDouble("startY", 80);
                                          ob.x = ob.startX;
                                          ob.y = ob.startY;
                                          ob.visible = o.optBoolean("visible", true);
                                          ob.say = o.optString("say", "");

                                          JSONArray blocks = o.optJSONArray("blocks");
                                          if(blocks != null){
                                              for(int z=0;z<blocks.length();z++){
                                                  JSONObject b = blocks.getJSONObject(z);
                                                  ob.blocks.add(new BlockItem(
                                                      b.optString("type"),
                                                      b.optString("label"),
                                                      b.optString("arg1"),
                                                      b.optString("arg2")
                                                  ));
                                              }
                                          }

                                          sc.objects.add(ob);
                                      }
                                  }

                                  pr.scenes.add(sc);
                              }
                          }

                          out.add(pr);
                      }
                  }catch(Exception ignored){}
                  return out;
              }

              public static void save(Context c, ArrayList<ProjectItem> projects){
                  try{
                      JSONArray arr = new JSONArray();
                      for(ProjectItem pr : projects){
                          JSONObject p = new JSONObject();
                          p.put("name", pr.name);

                          JSONArray scenes = new JSONArray();
                          for(SceneItem sc : pr.scenes){
                              JSONObject s = new JSONObject();
                              s.put("name", sc.name);
                              s.put("bgColor", sc.bgColor);

                              JSONArray objs = new JSONArray();
                              for(ObjectItem ob : sc.objects){
                                  JSONObject o = new JSONObject();
                                  o.put("name", ob.name);
                                  o.put("emoji", ob.emoji);
                                  o.put("imageUri", ob.imageUri);
                                  o.put("startX", ob.startX);
                                  o.put("startY", ob.startY);
                                  o.put("visible", ob.visible);
                                  o.put("say", ob.say);

                                  JSONArray blocks = new JSONArray();
                                  for(BlockItem b : ob.blocks){
                                      JSONObject bb = new JSONObject();
                                      bb.put("type", b.type);
                                      bb.put("label", b.label);
                                      bb.put("arg1", b.arg1);
                                      bb.put("arg2", b.arg2);
                                      blocks.put(bb);
                                  }
                                  o.put("blocks", blocks);
                                  objs.put(o);
                              }

                              s.put("objects", objs);
                              scenes.put(s);
                          }

                          p.put("scenes", scenes);
                          arr.put(p);
                      }

                      FileWriter wr = new FileWriter(file(c));
                      wr.write(arr.toString());
                      wr.close();
                  }catch(Exception ignored){}
              }
          }
          STORE

          cat <<'RUNNER' > app/src/main/java/com/pocketru/fixed/ScriptRunner.java
          package com.pocketru.fixed;

          import android.os.Handler;
          import android.os.Looper;
          import java.util.ArrayList;

          public class ScriptRunner {
              public interface Host {
                  void renderPlayerScene();
                  boolean stopped();
              }

              public static void runScene(Host host, SceneItem scene){
                  for(ObjectItem obj : scene.objects){
                      runObjectStart(host, obj);
                  }
              }

              public static void runTapped(Host host, ObjectItem obj){
                  runObjectEvent(host, obj, "event_tap");
              }

              private static void runObjectStart(Host host, ObjectItem obj){
                  ArrayList<BlockItem> seq = collectEvent(obj, "event_start");
                  if(seq.isEmpty()){
                      seq = collectLinear(obj);
                  }
                  execute(host, obj, seq);
              }

              private static void runObjectEvent(Host host, ObjectItem obj, String eventType){
                  ArrayList<BlockItem> seq = collectEvent(obj, eventType);
                  execute(host, obj, seq);
              }

              private static ArrayList<BlockItem> collectLinear(ObjectItem obj){
                  ArrayList<BlockItem> out = new ArrayList<>();
                  for(BlockItem b : obj.blocks){
                      if(!b.type.startsWith("event_")) out.add(b);
                  }
                  return out;
              }

              private static ArrayList<BlockItem> collectEvent(ObjectItem obj, String type){
                  ArrayList<BlockItem> out = new ArrayList<>();
                  boolean cap = false;
                  for(BlockItem b : obj.blocks){
                      if(type.equals(b.type)){
                          cap = true;
                          continue;
                      }
                      if(cap && b.type.startsWith("event_")) break;
                      if(cap) out.add(b);
                  }
                  return out;
              }

              private static void execute(Host host, ObjectItem obj, ArrayList<BlockItem> seq){
                  Handler h = new Handler(Looper.getMainLooper());
                  long delay = 0;

                  for(BlockItem b : seq){
                      switch (b.type){
                          case "move_x": {
                              final float dx = parse(b.arg1, 20f);
                              h.postDelayed(() -> {
                                  if(host.stopped()) return;
                                  obj.x += dx;
                                  host.renderPlayerScene();
                              }, delay);
                              break;
                          }
                          case "move_y": {
                              final float dy = parse(b.arg1, 20f);
                              h.postDelayed(() -> {
                                  if(host.stopped()) return;
                                  obj.y += dy;
                                  host.renderPlayerScene();
                              }, delay);
                              break;
                          }
                          case "go_xy": {
                              final float x = parse(b.arg1, 100f);
                              final float y = parse(b.arg2, 80f);
                              h.postDelayed(() -> {
                                  if(host.stopped()) return;
                                  obj.x = x;
                                  obj.y = y;
                                  host.renderPlayerScene();
                              }, delay);
                              break;
                          }
                          case "say": {
                              final String txt = b.arg1 == null ? "" : b.arg1;
                              h.postDelayed(() -> {
                                  if(host.stopped()) return;
                                  obj.say = txt;
                                  host.renderPlayerScene();
                              }, delay);
                              break;
                          }
                          case "hide": {
                              h.postDelayed(() -> {
                                  if(host.stopped()) return;
                                  obj.visible = false;
                                  host.renderPlayerScene();
                              }, delay);
                              break;
                          }
                          case "show": {
                              h.postDelayed(() -> {
                                  if(host.stopped()) return;
                                  obj.visible = true;
                                  host.renderPlayerScene();
                              }, delay);
                              break;
                          }
                          case "wait": {
                              delay += (long)(parse(b.arg1, 1f) * 1000f);
                              break;
                          }
                      }
                  }
              }

              private static float parse(String s, float d){
                  try { return Float.parseFloat(s); } catch(Exception e){ return d; }
              }
          }
          RUNNER

          cat <<'MAINACT' > app/src/main/java/com/pocketru/fixed/MainActivity.java
          package com.pocketru.fixed;

          import android.app.AlertDialog;
          import android.content.Intent;
          import android.graphics.Color;
          import android.graphics.Typeface;
          import android.graphics.drawable.GradientDrawable;
          import android.net.Uri;
          import android.os.Bundle;
          import android.view.View;
          import android.widget.Button;
          import android.widget.EditText;
          import android.widget.FrameLayout;
          import android.widget.ImageView;
          import android.widget.LinearLayout;
          import android.widget.TextView;
          import android.widget.Toast;
          import androidx.activity.result.ActivityResultLauncher;
          import androidx.activity.result.contract.ActivityResultContracts;
          import androidx.appcompat.app.AppCompatActivity;
          import java.util.ArrayList;

          public class MainActivity extends AppCompatActivity implements ScriptRunner.Host {
              private View screenProjects, screenScenes, screenObjects, screenScripts, screenPlayer;
              private LinearLayout projectsList, scenesList, objectsList, blocksList;
              private FrameLayout playerStage, playerStageLayer;
              private TextView txtProjectTitle, txtSceneTitle, txtObjectTitle;

              private final ArrayList<ProjectItem> projects = new ArrayList<>();
              private int currentProject = -1, currentScene = -1, currentObject = -1;
              private int pendingImportProject = -1, pendingImportScene = -1, pendingImportObject = -1;
              private boolean stopFlag = false;

              private ActivityResultLauncher<Intent> imagePicker;

              @Override
              public void renderPlayerScene() {
                  runOnUiThread(this::drawPlayerScene);
              }

              @Override
              public boolean stopped() {
                  return stopFlag;
              }

              @Override
              protected void onCreate(Bundle savedInstanceState) {
                  super.onCreate(savedInstanceState);
                  setContentView(R.layout.activity_main);

                  screenProjects = findViewById(R.id.screen_projects);
                  screenScenes = findViewById(R.id.screen_scenes);
                  screenObjects = findViewById(R.id.screen_objects);
                  screenScripts = findViewById(R.id.screen_scripts);
                  screenPlayer = findViewById(R.id.screen_player);

                  projectsList = findViewById(R.id.projects_list);
                  scenesList = findViewById(R.id.scenes_list);
                  objectsList = findViewById(R.id.objects_list);
                  blocksList = findViewById(R.id.blocks_list);

                  playerStage = findViewById(R.id.player_stage);
                  playerStageLayer = findViewById(R.id.player_stage_layer);

                  txtProjectTitle = findViewById(R.id.txt_project_title);
                  txtSceneTitle = findViewById(R.id.txt_scene_title);
                  txtObjectTitle = findViewById(R.id.txt_object_title);

                  projects.addAll(ProjectStore.load(this));

                  imagePicker = registerForActivityResult(new ActivityResultContracts.StartActivityForResult(), res -> {
                      if(res.getResultCode() == RESULT_OK && res.getData() != null && res.getData().getData() != null){
                          try{
                              Uri uri = res.getData().getData();
                              getContentResolver().takePersistableUriPermission(uri, Intent.FLAG_GRANT_READ_URI_PERMISSION);

                              if(pendingImportProject >= 0 && pendingImportScene >= 0 && pendingImportObject >= 0){
                                  projects.get(pendingImportProject)
                                          .scenes.get(pendingImportScene)
                                          .objects.get(pendingImportObject)
                                          .imageUri = uri.toString();
                                  saveAll();
                                  renderObjects();
                              }
                          }catch(Exception e){
                              toast("Ошибка импорта");
                          }
                      }
                  });

                  findViewById(R.id.btn_new_project).setOnClickListener(v -> createProjectDialog());
                  findViewById(R.id.back_to_projects).setOnClickListener(v -> showProjects());
                  findViewById(R.id.back_to_scenes).setOnClickListener(v -> showScenes());
                  findViewById(R.id.back_to_objects).setOnClickListener(v -> showObjects());

                  findViewById(R.id.btn_new_scene).setOnClickListener(v -> createSceneDialog());
                  findViewById(R.id.btn_new_object).setOnClickListener(v -> createObjectDialog());

                  findViewById(R.id.btn_play_scene).setOnClickListener(v -> startPlayer());
                  findViewById(R.id.btn_stop_player).setOnClickListener(v -> stopPlayer());

                  findViewById(R.id.btn_add_block).setOnClickListener(v -> showCategoryDialog());

                  renderProjects();
                  showProjects();
              }

              private void toast(String s){
                  Toast.makeText(this, s, Toast.LENGTH_SHORT).show();
              }

              private void saveAll(){
                  ProjectStore.save(this, projects);
              }

              private ProjectItem currentProject(){
                  if(currentProject < 0 || currentProject >= projects.size()) return null;
                  return projects.get(currentProject);
              }

              private SceneItem currentScene(){
                  ProjectItem p = currentProject();
                  if(p == null || currentScene < 0 || currentScene >= p.scenes.size()) return null;
                  return p.scenes.get(currentScene);
              }

              private ObjectItem currentObject(){
                  SceneItem s = currentScene();
                  if(s == null || currentObject < 0 || currentObject >= s.objects.size()) return null;
                  return s.objects.get(currentObject);
              }

              private void showProjects(){
                  screenProjects.setVisibility(View.VISIBLE);
                  screenScenes.setVisibility(View.GONE);
                  screenObjects.setVisibility(View.GONE);
                  screenScripts.setVisibility(View.GONE);
                  screenPlayer.setVisibility(View.GONE);
                  renderProjects();
              }

              private void showScenes(){
                  screenProjects.setVisibility(View.GONE);
                  screenScenes.setVisibility(View.VISIBLE);
                  screenObjects.setVisibility(View.GONE);
                  screenScripts.setVisibility(View.GONE);
                  screenPlayer.setVisibility(View.GONE);
                  renderScenes();
              }

              private void showObjects(){
                  screenProjects.setVisibility(View.GONE);
                  screenScenes.setVisibility(View.GONE);
                  screenObjects.setVisibility(View.VISIBLE);
                  screenScripts.setVisibility(View.GONE);
                  screenPlayer.setVisibility(View.GONE);
                  renderObjects();
              }

              private void showScripts(){
                  screenProjects.setVisibility(View.GONE);
                  screenScenes.setVisibility(View.GONE);
                  screenObjects.setVisibility(View.GONE);
                  screenScripts.setVisibility(View.VISIBLE);
                  screenPlayer.setVisibility(View.GONE);
                  renderBlocks();
              }

              private void startPlayer(){
                  SceneItem scene = currentScene();
                  if(scene == null) return;

                  for(ObjectItem o : scene.objects){
                      o.x = o.startX;
                      o.y = o.startY;
                      o.visible = true;
                      o.say = "";
                  }

                  stopFlag = false;
                  screenProjects.setVisibility(View.GONE);
                  screenScenes.setVisibility(View.GONE);
                  screenObjects.setVisibility(View.GONE);
                  screenScripts.setVisibility(View.GONE);
                  screenPlayer.setVisibility(View.VISIBLE);

                  drawPlayerScene();
                  ScriptRunner.runScene(this, scene);
              }

              private void stopPlayer(){
                  stopFlag = true;
                  showObjects();
              }

              private void renderProjects(){
                  projectsList.removeAllViews();
                  for(int i=0;i<projects.size();i++){
                      ProjectItem p = projects.get(i);
                      LinearLayout card = makeCard();
                      card.addView(title(p.name));
                      card.addView(subtitle("Сцен: " + p.scenes.size()));

                      final int idx = i;
                      card.setOnClickListener(v -> {
                          currentProject = idx;
                          currentScene = -1;
                          currentObject = -1;
                          showScenes();
                      });

                      card.setOnLongClickListener(v -> {
                          showProjectMenu(idx);
                          return true;
                      });

                      projectsList.addView(card);
                  }
              }

              private void renderScenes(){
                  ProjectItem p = currentProject();
                  if(p == null) return;
                  txtProjectTitle.setText(p.name + " / Сцены");

                  scenesList.removeAllViews();
                  for(int i=0;i<p.scenes.size();i++){
                      SceneItem s = p.scenes.get(i);
                      LinearLayout card = makeCard();
                      card.addView(title(s.name));
                      card.addView(subtitle("Объектов: " + s.objects.size()));

                      final int idx = i;
                      card.setOnClickListener(v -> {
                          currentScene = idx;
                          currentObject = -1;
                          showObjects();
                      });

                      card.setOnLongClickListener(v -> {
                          showSceneMenu(idx);
                          return true;
                      });

                      scenesList.addView(card);
                  }
              }

              private void renderObjects(){
                  SceneItem s = currentScene();
                  if(s == null) return;
                  txtSceneTitle.setText(s.name + " / Объекты");

                  objectsList.removeAllViews();
                  for(int i=0;i<s.objects.size();i++){
                      ObjectItem o = s.objects.get(i);

                      LinearLayout card = makeCard();
                      card.addView(title(o.name + " " + o.emoji));
                      card.addView(subtitle(o.imageUri.isEmpty() ? "Спрайт: emoji" : "Спрайт: изображение"));

                      LinearLayout row = new LinearLayout(this);
                      row.setOrientation(LinearLayout.HORIZONTAL);

                      Button importBtn = new Button(this);
                      importBtn.setText("Импорт");
                      importBtn.setTextColor(Color.WHITE);
                      importBtn.setBackgroundResource(R.drawable.btn_purple);

                      Button scriptBtn = new Button(this);
                      scriptBtn.setText("Скрипты");
                      scriptBtn.setTextColor(Color.WHITE);
                      scriptBtn.setBackgroundResource(R.drawable.btn_blue);

                      LinearLayout.LayoutParams lp1 = new LinearLayout.LayoutParams(0, dp(40), 1f);
                      LinearLayout.LayoutParams lp2 = new LinearLayout.LayoutParams(0, dp(40), 1f);
                      lp2.leftMargin = dp(8);

                      importBtn.setLayoutParams(lp1);
                      scriptBtn.setLayoutParams(lp2);

                      final int idx = i;
                      importBtn.setOnClickListener(v -> importSprite(idx));
                      scriptBtn.setOnClickListener(v -> {
                          currentObject = idx;
                          showScripts();
                      });

                      row.addView(importBtn);
                      row.addView(scriptBtn);
                      card.addView(row);

                      card.setOnLongClickListener(v -> {
                          showObjectMenu(idx);
                          return true;
                      });

                      objectsList.addView(card);
                  }
              }

              private void renderBlocks(){
                  ObjectItem o = currentObject();
                  if(o == null) return;
                  txtObjectTitle.setText(o.name + " / Скрипты");

                  blocksList.removeAllViews();
                  for(int i=0;i<o.blocks.size();i++){
                      BlockItem b = o.blocks.get(i);

                      TextView tv = new TextView(this);
                      String text = b.label;
                      if(b.arg1 != null && !b.arg1.isEmpty()) text += " " + b.arg1;
                      if(b.arg2 != null && !b.arg2.isEmpty()) text += " " + b.arg2;
                      tv.setText(text);
                      tv.setTextColor(Color.WHITE);
                      tv.setTextSize(16f);
                      tv.setPadding(dp(14), dp(10), dp(14), dp(10));

                      if(b.type.startsWith("event_")) tv.setBackgroundResource(R.drawable.block_event);
                      else if(b.type.startsWith("move") || "go_xy".equals(b.type)) tv.setBackgroundResource(R.drawable.block_motion);
                      else if("wait".equals(b.type)) tv.setBackgroundResource(R.drawable.block_control);
                      else tv.setBackgroundResource(R.drawable.block_looks);

                      LinearLayout.LayoutParams lp = new LinearLayout.LayoutParams(
                          LinearLayout.LayoutParams.MATCH_PARENT,
                          LinearLayout.LayoutParams.WRAP_CONTENT
                      );
                      lp.topMargin = dp(8);
                      tv.setLayoutParams(lp);

                      final int idx = i;
                      tv.setOnClickListener(v -> showBlockMenu(idx));

                      blocksList.addView(tv);
                  }
              }

              private void drawPlayerScene(){
                  SceneItem scene = currentScene();
                  if(scene == null) return;

                  try{
                      GradientDrawable bg = new GradientDrawable();
                      bg.setColor(Color.parseColor(scene.bgColor));
                      bg.setCornerRadius(dp(18));
                      bg.setStroke(dp(2), Color.parseColor("#8AC4E8"));
                      playerStage.setBackground(bg);
                  }catch(Exception ignored){}

                  playerStageLayer.removeAllViews();

                  for(int i=0;i<scene.objects.size();i++){
                      ObjectItem o = scene.objects.get(i);

                      FrameLayout wrap = new FrameLayout(this);
                      wrap.setTranslationX(o.x);
                      wrap.setTranslationY(o.y);

                      View spriteView;
                      if(o.imageUri != null && !o.imageUri.isEmpty()){
                          ImageView img = new ImageView(this);
                          img.setImageURI(Uri.parse(o.imageUri));
                          img.setAdjustViewBounds(true);
                          FrameLayout.LayoutParams ilp = new FrameLayout.LayoutParams(dp(72), dp(72));
                          img.setLayoutParams(ilp);
                          spriteView = img;
                      } else {
                          TextView tv = new TextView(this);
                          tv.setText(o.emoji);
                          tv.setTextSize(42f);
                          spriteView = tv;
                      }

                      spriteView.setVisibility(o.visible ? View.VISIBLE : View.INVISIBLE);

                      final int idx = i;
                      spriteView.setOnClickListener(v -> {
                          if(!stopFlag){
                              ScriptRunner.runTapped(this, scene.objects.get(idx));
                          }
                      });

                      wrap.addView(spriteView);

                      if(o.say != null && !o.say.isEmpty()){
                          TextView speech = new TextView(this);
                          speech.setText(o.say);
                          speech.setTextSize(14f);
                          speech.setTextColor(Color.BLACK);
                          speech.setPadding(dp(8), dp(4), dp(8), dp(4));
                          speech.setBackgroundResource(R.drawable.bg_panel);

                          FrameLayout.LayoutParams slp = new FrameLayout.LayoutParams(
                              FrameLayout.LayoutParams.WRAP_CONTENT,
                              FrameLayout.LayoutParams.WRAP_CONTENT
                          );
                          slp.leftMargin = dp(40);
                          slp.topMargin = -dp(10);
                          speech.setLayoutParams(slp);
                          wrap.addView(speech);
                      }

                      playerStageLayer.addView(wrap);
                  }
              }

              private void createProjectDialog(){
                  EditText input = new EditText(this);
                  input.setHint("Название проекта");
                  input.setText("Мой проект");

                  new AlertDialog.Builder(this)
                      .setTitle("Создать проект")
                      .setView(input)
                      .setPositiveButton("Создать", (d,w) -> {
                          ProjectItem p = new ProjectItem();
                          p.name = input.getText().toString().trim().isEmpty() ? "Проект" : input.getText().toString();
                          projects.add(p);
                          saveAll();
                          renderProjects();
                      })
                      .setNegativeButton("Отмена", null)
                      .show();
              }

              private void createSceneDialog(){
                  ProjectItem p = currentProject();
                  if(p == null) return;

                  LinearLayout root = new LinearLayout(this);
                  root.setOrientation(LinearLayout.VERTICAL);

                  EditText name = new EditText(this);
                  name.setHint("Название сцены");
                  name.setText("Сцена " + (p.scenes.size()+1));

                  EditText color = new EditText(this);
                  color.setHint("#D8F0FF");
                  color.setText("#D8F0FF");

                  root.addView(name);
                  root.addView(color);

                  new AlertDialog.Builder(this)
                      .setTitle("Создать сцену")
                      .setView(root)
                      .setPositiveButton("Создать", (d,w) -> {
                          SceneItem s = new SceneItem();
                          s.name = name.getText().toString().trim().isEmpty() ? "Сцена" : name.getText().toString();
                          s.bgColor = color.getText().toString().trim().isEmpty() ? "#D8F0FF" : color.getText().toString();
                          p.scenes.add(s);
                          saveAll();
                          renderScenes();
                      })
                      .setNegativeButton("Отмена", null)
                      .show();
              }

              private void createObjectDialog(){
                  SceneItem s = currentScene();
                  if(s == null) return;

                  LinearLayout root = new LinearLayout(this);
                  root.setOrientation(LinearLayout.VERTICAL);

                  EditText name = new EditText(this);
                  name.setHint("Название объекта");
                  name.setText("Объект " + (s.objects.size()+1));

                  EditText emoji = new EditText(this);
                  emoji.setHint("Emoji");
                  emoji.setText("🐱");

                  EditText x = new EditText(this);
                  x.setHint("Старт X");
                  x.setText("100");

                  EditText y = new EditText(this);
                  y.setHint("Старт Y");
                  y.setText("80");

                  root.addView(name);
                  root.addView(emoji);
                  root.addView(x);
                  root.addView(y);

                  new AlertDialog.Builder(this)
                      .setTitle("Создать объект")
                      .setView(root)
                      .setPositiveButton("Создать", (d,w) -> {
                          ObjectItem o = new ObjectItem();
                          o.name = name.getText().toString().trim().isEmpty() ? "Объект" : name.getText().toString();
                          o.emoji = emoji.getText().toString().trim().isEmpty() ? "🐱" : emoji.getText().toString();
                          o.startX = parseFloat(x.getText().toString(), 100f);
                          o.startY = parseFloat(y.getText().toString(), 80f);
                          o.x = o.startX;
                          o.y = o.startY;
                          s.objects.add(o);
                          saveAll();
                          renderObjects();
                      })
                      .setNeutralButton("Создать и импортировать", (d,w) -> {
                          ObjectItem o = new ObjectItem();
                          o.name = name.getText().toString().trim().isEmpty() ? "Объект" : name.getText().toString();
                          o.emoji = emoji.getText().toString().trim().isEmpty() ? "🐱" : emoji.getText().toString();
                          o.startX = parseFloat(x.getText().toString(), 100f);
                          o.startY = parseFloat(y.getText().toString(), 80f);
                          o.x = o.startX;
                          o.y = o.startY;
                          s.objects.add(o);
                          saveAll();

                          pendingImportProject = currentProject;
                          pendingImportScene = currentScene;
                          pendingImportObject = s.objects.size()-1;
                          openImagePicker();

                          renderObjects();
                      })
                      .setNegativeButton("Отмена", null)
                      .show();
              }

              private void importSprite(int objectIndex){
                  pendingImportProject = currentProject;
                  pendingImportScene = currentScene;
                  pendingImportObject = objectIndex;
                  openImagePicker();
              }

              private void openImagePicker(){
                  Intent i = new Intent(Intent.ACTION_OPEN_DOCUMENT);
                  i.addCategory(Intent.CATEGORY_OPENABLE);
                  i.setType("image/*");
                  i.addFlags(Intent.FLAG_GRANT_READ_URI_PERMISSION);
                  i.addFlags(Intent.FLAG_GRANT_PERSISTABLE_URI_PERMISSION);
                  imagePicker.launch(i);
              }

              private void showCategoryDialog(){
                  String[] cats = {"События", "Движение", "Внешность", "Управление"};
                  new AlertDialog.Builder(this)
                      .setTitle("Категория")
                      .setItems(cats, (d,w) -> {
                          if(w == 0) showBlockTypeDialog("event");
                          else if(w == 1) showBlockTypeDialog("motion");
                          else if(w == 2) showBlockTypeDialog("looks");
                          else showBlockTypeDialog("control");
                      })
                      .show();
              }

              private void showBlockTypeDialog(String cat){
                  if("event".equals(cat)){
                      String[] items = {"Когда запущено", "Когда объект нажат"};
                      new AlertDialog.Builder(this)
                          .setTitle("События")
                          .setItems(items, (d,w) -> {
                              if(w == 0) insertBlockDialog(new BlockItem("event_start", "Когда запущено", "", ""));
                              else insertBlockDialog(new BlockItem("event_tap", "Когда объект нажат", "", ""));
                          }).show();
                  } else if("motion".equals(cat)){
                      String[] items = {"Двигаться по X", "Двигаться по Y", "Перейти в X Y"};
                      new AlertDialog.Builder(this)
                          .setTitle("Движение")
                          .setItems(items, (d,w) -> {
                              if(w == 0) askOneArg("Двигаться по X", "20", v -> insertBlockDialog(new BlockItem("move_x", "Двигаться по X", v, "")));
                              else if(w == 1) askOneArg("Двигаться по Y", "20", v -> insertBlockDialog(new BlockItem("move_y", "Двигаться по Y", v, "")));
                              else askTwoArg("Перейти в X Y", "100", "80", (a,b) -> insertBlockDialog(new BlockItem("go_xy", "Перейти в", a, b)));
                          }).show();
                  } else if("looks".equals(cat)){
                      String[] items = {"Сказать", "Скрыть", "Показать"};
                      new AlertDialog.Builder(this)
                          .setTitle("Внешность")
                          .setItems(items, (d,w) -> {
                              if(w == 0) askOneArg("Сказать", "Привет!", v -> insertBlockDialog(new BlockItem("say", "Сказать", v, "")));
                              else if(w == 1) insertBlockDialog(new BlockItem("hide", "Скрыть", "", ""));
                              else insertBlockDialog(new BlockItem("show", "Показать", "", ""));
                          }).show();
                  } else if("control".equals(cat)){
                      String[] items = {"Ждать"};
                      new AlertDialog.Builder(this)
                          .setTitle("Управление")
                          .setItems(items, (d,w) -> askOneArg("Ждать", "1", v -> insertBlockDialog(new BlockItem("wait", "Ждать", v, ""))))
                          .show();
                  }
              }

              private interface OneArg { void ok(String v); }
              private interface TwoArg { void ok(String a, String b); }

              private void askOneArg(String title, String def, OneArg cb){
                  EditText input = new EditText(this);
                  input.setText(def);
                  new AlertDialog.Builder(this)
                      .setTitle(title)
                      .setView(input)
                      .setPositiveButton("ОК", (d,w) -> cb.ok(input.getText().toString()))
                      .setNegativeButton("Отмена", null)
                      .show();
              }

              private void askTwoArg(String title, String d1, String d2, TwoArg cb){
                  LinearLayout root = new LinearLayout(this);
                  root.setOrientation(LinearLayout.VERTICAL);
                  EditText a = new EditText(this);
                  EditText b = new EditText(this);
                  a.setHint("X");
                  b.setHint("Y");
                  a.setText(d1);
                  b.setText(d2);
                  root.addView(a);
                  root.addView(b);

                  new AlertDialog.Builder(this)
                      .setTitle(title)
                      .setView(root)
                      .setPositiveButton("ОК", (d,w) -> cb.ok(a.getText().toString(), b.getText().toString()))
                      .setNegativeButton("Отмена", null)
                      .show();
              }

              private void insertBlockDialog(BlockItem block){
                  ObjectItem o = currentObject();
                  if(o == null) return;

                  String[] opts = new String[o.blocks.size()+1];
                  opts[0] = "В конец";
                  for(int i=0;i<o.blocks.size();i++){
                      opts[i+1] = "Перед: " + o.blocks.get(i).label;
                  }

                  new AlertDialog.Builder(this)
                      .setTitle("Куда вставить блок?")
                      .setItems(opts, (d,w) -> {
                          if(w == 0) o.blocks.add(block);
                          else o.blocks.add(w-1, block);
                          saveAll();
                          renderBlocks();
                      })
                      .show();
              }

              private void showBlockMenu(int idx){
                  ObjectItem o = currentObject();
                  if(o == null) return;

                  String[] items = {"Удалить"};
                  new AlertDialog.Builder(this)
                      .setTitle("Блок")
                      .setItems(items, (d,w) -> {
                          o.blocks.remove(idx);
                          saveAll();
                          renderBlocks();
                      }).show();
              }

              private void showProjectMenu(int idx){
                  String[] items = {"Переименовать", "Удалить"};
                  new AlertDialog.Builder(this)
                      .setTitle(projects.get(idx).name)
                      .setItems(items, (d,w) -> {
                          if(w == 0){
                              EditText input = new EditText(this);
                              input.setText(projects.get(idx).name);
                              new AlertDialog.Builder(this)
                                  .setTitle("Переименовать проект")
                                  .setView(input)
                                  .setPositiveButton("ОК", (dd,ww) -> {
                                      projects.get(idx).name = input.getText().toString();
                                      saveAll();
                                      renderProjects();
                                  })
                                  .setNegativeButton("Отмена", null)
                                  .show();
                          } else {
                              projects.remove(idx);
                              saveAll();
                              renderProjects();
                          }
                      }).show();
              }

              private void showSceneMenu(int idx){
                  ProjectItem p = currentProject();
                  if(p == null) return;
                  String[] items = {"Переименовать", "Удалить"};
                  new AlertDialog.Builder(this)
                      .setTitle(p.scenes.get(idx).name)
                      .setItems(items, (d,w) -> {
                          if(w == 0){
                              EditText input = new EditText(this);
                              input.setText(p.scenes.get(idx).name);
                              new AlertDialog.Builder(this)
                                  .setTitle("Переименовать сцену")
                                  .setView(input)
                                  .setPositiveButton("ОК", (dd,ww) -> {
                                      p.scenes.get(idx).name = input.getText().toString();
                                      saveAll();
                                      renderScenes();
                                  })
                                  .setNegativeButton("Отмена", null)
                                  .show();
                          } else {
                              p.scenes.remove(idx);
                              saveAll();
                              renderScenes();
                          }
                      }).show();
              }

              private void showObjectMenu(int idx){
                  SceneItem s = currentScene();
                  if(s == null) return;
                  String[] items = {"Переименовать", "Удалить"};
                  new AlertDialog.Builder(this)
                      .setTitle(s.objects.get(idx).name)
                      .setItems(items, (d,w) -> {
                          if(w == 0){
                              EditText input = new EditText(this);
                              input.setText(s.objects.get(idx).name);
                              new AlertDialog.Builder(this)
                                  .setTitle("Переименовать объект")
                                  .setView(input)
                                  .setPositiveButton("ОК", (dd,ww) -> {
                                      s.objects.get(idx).name = input.getText().toString();
                                      saveAll();
                                      renderObjects();
                                  })
                                  .setNegativeButton("Отмена", null)
                                  .show();
                          } else {
                              s.objects.remove(idx);
                              saveAll();
                              renderObjects();
                          }
                      }).show();
              }

              private LinearLayout makeCard(){
                  LinearLayout card = new LinearLayout(this);
                  card.setOrientation(LinearLayout.VERTICAL);
                  card.setBackgroundResource(R.drawable.bg_panel);
                  card.setPadding(dp(12), dp(12), dp(12), dp(12));
                  LinearLayout.LayoutParams lp = new LinearLayout.LayoutParams(
                      LinearLayout.LayoutParams.MATCH_PARENT,
                      LinearLayout.LayoutParams.WRAP_CONTENT
                  );
                  lp.topMargin = dp(10);
                  card.setLayoutParams(lp);
                  return card;
              }

              private TextView title(String s){
                  TextView tv = new TextView(this);
                  tv.setText(s);
                  tv.setTextSize(18f);
                  tv.setTextColor(Color.parseColor("#2E648F"));
                  tv.setTypeface(null, Typeface.BOLD);
                  return tv;
              }

              private TextView subtitle(String s){
                  TextView tv = new TextView(this);
                  tv.setText(s);
                  tv.setTextSize(13f);
                  tv.setTextColor(Color.parseColor("#5D88A8"));
                  return tv;
              }

              private int dp(int v){
                  return (int)(v * getResources().getDisplayMetrics().density);
              }

              private float parseFloat(String s, float d){
                  try { return Float.parseFloat(s); } catch(Exception e){ return d; }
              }
          }
          MAINACT

          cat <<EOF > app/src/main/AndroidManifest.xml
          <?xml version="1.0" encoding="utf-8"?>
          <manifest xmlns:android="http://schemas.android.com/apk/res/android">
              <application
                  android:label="Карманный Код"
                  android:theme="@style/AppTheme">
                  <activity
                      android:name="com.pocketru.fixed.MainActivity"
                      android:exported="true">
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN"/>
                          <category android:name="android.intent.category.LAUNCHER"/>
                      </intent-filter>
                  </activity>
              </application>
          </manifest>
          EOF

          echo "include ':app'" > settings.gradle

          cat <<EOF > build.gradle
          buildscript {
              repositories { google(); mavenCentral() }
              dependencies { classpath 'com.android.tools.build:gradle:8.2.2' }
          }
          allprojects {
              repositories { google(); mavenCentral() }
          }
          EOF

          cat <<EOF > app/build.gradle
          plugins {
              id 'com.android.application'
          }

          android {
              namespace 'com.pocketru.fixed'
              compileSdk 34

              defaultConfig {
                  applicationId 'com.pocketru.fixed'
                  minSdk 24
                  targetSdk 34
              }

              buildFeatures {
                  buildConfig true
              }
          }

          dependencies {
              implementation 'androidx.appcompat:appcompat:1.6.1'
          }
          EOF

      - name: Build APK
        run: |
          gradle wrapper --gradle-version 8.5
          ./gradlew assembleDebug

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: Pocket_Code_RU_Fixed_APK
          path: app/build/outputs/apk/debug/app-debug.apk
